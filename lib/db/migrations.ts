/**
 * Migrations — Costa Inteligente
 * Con PostgreSQL las migraciones se aplican via drizzle-kit:
 *   npx drizzle-kit push
 * Este archivo existe solo para mantener compatibilidad de imports.
 */
export {};
